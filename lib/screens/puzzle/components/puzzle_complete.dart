import '../../../knowit_exporter.dart';

class PuzzleComplete extends ConsumerWidget {
  const PuzzleComplete({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return Container();
  }
}